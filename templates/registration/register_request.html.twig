{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
    <h1>Prośba o dostęp</h1>

    {% if message is defined %}
        <h2>{{ message }}</h2>
    {% endif %}
    <section class="login-form">
        <div>
            {{ form(form) }}
        </div>
    </section>
{% endblock %}

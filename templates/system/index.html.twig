{% extends 'base.html.twig' %}

{% block title %}{{ caption }}{% endblock %}

{% block body %}
    {{ parent () }}
    <section class="normal-form">
        <h1>{{ caption }}</h1>
        {{ form(form) }}
        {% if error is defined %}
        {{ error }}
        {% endif %}
    </section>
{% endblock %}
